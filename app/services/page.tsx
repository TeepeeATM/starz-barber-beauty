import type { Metadata } from "next";
import { ServiceList } from "@/components/ServiceList";
import { bookingUrl, services } from "@/data/site";
export const metadata: Metadata = { title:"Services & Pricing", description:"Haircuts, beard care, shaves, color and natural hair services at Starz Barber & Beauty in Horn Lake." };
export default function Services(){const groups=[...new Set(services.map(s=>s.group))];return <><section className="page-hero"><p className="eyebrow">SERVICES &amp; PRICING</p><h1>Know what you’re booking before you sit down</h1><p>Times are set aside for the work listed. Services marked “+” can vary with length, density or the finish you choose.</p></section><section className="section narrow">{groups.map(group=><div className="service-group" key={group}><h2>{group}</h2><ServiceListFor group={group}/></div>)}<div className="policy-note"><h2>Need a time outside regular hours?</h2><p>Early and after-hours appointments may be available with a gratuity starting at $20. Check Booksy or contact the shop before arriving.</p></div><a className="button" href={bookingUrl}>Choose a service on Booksy</a></section></>}
function ServiceListFor({group}:{group:string}){const subset=services.filter(s=>s.group===group);return <div className="service-list">{subset.map(s=><article className="service-row" key={s.name}><div><h3>{s.name}</h3><p>{s.note}</p></div><div className="service-meta"><strong>{s.price}</strong><span>{s.time}</span></div></article>)}</div>}
